<Window x:Class="Mint.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="450" Width="800"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#3C3D37" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <!-- Custom Title Bar -->
        <DockPanel Background="Black" Height="30" VerticalAlignment="Top" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <TextBlock Text="  Mint" FontSize="14" FontWeight="Bold" Foreground="#58DA06" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="−" Width="30" Height="30" Background="Black" Foreground="White" BorderBrush="Black" Click="Minimize_Click"/>
                <Button Content="X" Width="30" Height="30" Background="Black" Foreground="White" BorderBrush="Black" Click="Close_Click"/>
            </StackPanel>
        </DockPanel>

        <!-- Main Layout -->
        <Grid Margin="30,50,30,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Images -->
            <Image Margin="242,105,34,-45" Source="/Images/mint.png" Stretch="Fill" Grid.ColumnSpan="2"/>
            <Image Margin="128,105,148,-45" Source="/Images/mint.png" Stretch="Fill" Grid.ColumnSpan="2"/>
            <Image Margin="10,105,266,-45" Source="/Images/mint.png" Stretch="Fill" Grid.ColumnSpan="2"/>

            <!-- Login Form -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,40,0">
                <TextBlock Text="Login" FontSize="48" FontWeight="Bold" Foreground="White" Margin="0,0,0,40"/>

                <!-- Username -->
                <TextBlock Text="username" Foreground="#aaaaaa" FontSize="16"/>
                <Border BorderBrush="White" BorderThickness="0 0 0 1" Margin="0,0,0,20">
                    <TextBox x:Name="UsernameTextBox" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" Padding="0,5,0,5"/>
                </Border>

                <!-- Password -->
                <TextBlock Text="password" Foreground="#aaaaaa" FontSize="16"/>
                <Border BorderBrush="White" BorderThickness="0 0 0 1" Margin="0,0,0,30">
                    <PasswordBox x:Name="PasswordBox" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="16" Padding="0,5,0,5" Width="256"/>
                </Border>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="Login" Width="80" Height="30" Background="#58DA06" Foreground="White" FontWeight="Bold" Margin="0,0,10,0" Click="Login_Click" IsDefault="True">
                        <Button.Template>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#70F91A"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#06DA1E"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="Gray"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Content="Register" Width="80" Height="30" Background="Gray" Foreground="White" FontWeight="Bold" Click="Register_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#999"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#777"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#555"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
